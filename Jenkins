pipeline {
  agent any
  
  environment {
      TOOL = tool name: 'micro-gw', type: 'com.cloudbees.jenkins.plugins.customtools.CustomTool/micro-gw/mgwtoolkit'
  }
  stages {
    stage('Build') {
      steps {
        echo 'Running build automation'
        sh 'micro-gw_HOME'
      }
    }
  }
}
